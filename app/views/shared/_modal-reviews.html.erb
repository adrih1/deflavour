

<!-- Modal -->
<div class="modal-dialog">
  <div class="modal-content-reviews">
    <div class="modal-body-reviews">

      <h5>Notez <%= spirit.name%></h5>
        <%= simple_form_for [spirit, Review.new] do |f| %>
          <%= f.input :content, placeholder: 'Décrivez votre experience avec ce spiritueux', label: "Commentaire", label_html: { class: 'form-label-review ' }, input_html: { class: 'form-input-review' } %>
          <%= f.input :rating, collection: Review::AUTHORIZED_RATINGS, include_blank: false, label_html: { class: 'form-label-review '}, input_html: { class: 'form-input-review'  } %>
          <%= f.submit "Submit review", class: "btn btn-classic-new", data: { controller: 'alert', action: 'click->alert#hop'} %>
        <% end %>
    </div>
  </div>
</div>
